% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curated.R
\name{get_labour_force_survey}
\alias{get_labour_force_survey}
\title{Get the Labour Force Survey (basic tabulation)}
\usage{
get_labour_force_survey(...)
}
\arguments{
\item{...}{
  Arguments passed on to \code{\link[=get_estat]{get_estat}}
  \describe{
    \item{\code{decode_labels}}{If \code{TRUE} (default), join metadata labels onto the coded
values. \code{FALSE} skips the metadata join entirely and returns just the coded
columns — a power-user fast path.}
    \item{\code{as_data_table}}{If \code{TRUE}, return the internal \code{data.table} directly
instead of converting to a tibble, for bulk-analysis users who don't want
even the boundary conversion. Defaults to \code{FALSE}.}
    \item{\code{limit}}{Maximum number of rows to return. \code{NULL} (default) returns all
matching rows.}
    \item{\code{key}}{e-Stat appId. Defaults to the stored key.}
  }}
}
\value{
The tidy tibble from \code{\link[=get_estat]{get_estat()}}.
}
\description{
Convenience wrapper for the Labour Force Survey table of 15-and-over
population by labour-force status. Equivalent to
\code{get_estat_curated("labour_force_survey", ...)}.
}
\examples{
\dontrun{
get_labour_force_survey(limit = 500)
}
}
