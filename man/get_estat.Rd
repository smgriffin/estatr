% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_estat.R
\name{get_estat}
\alias{get_estat}
\title{Get tidy, labelled data from e-Stat}
\usage{
get_estat(
  statsDataId,
  ...,
  decode_labels = TRUE,
  as_data_table = FALSE,
  limit = NULL,
  checkpoint = NULL,
  geometry = FALSE,
  geometry_level = "auto",
  geometry_year = 2020,
  geometry_datum = "2000",
  geometry_designated_cities = "both",
  lang = getOption("estatr.lang", "E"),
  key = get_estat_key()
)
}
\arguments{
\item{statsDataId}{The table id to retrieve (from \code{\link[=estat_stats_list]{estat_stats_list()}} or
\code{\link[=search_estat]{search_estat()}}).}

\item{...}{Filter parameters passed to \code{getStatsData} (e.g. \code{cdCat01},
\code{cdArea}, \code{cdTime}, \code{cdTimeFrom}, \code{cdTimeTo}).}

\item{decode_labels}{If \code{TRUE} (default), join metadata labels onto the coded
values. \code{FALSE} skips the metadata join entirely and returns just the coded
columns — a power-user fast path.}

\item{as_data_table}{If \code{TRUE}, return the internal \code{data.table} directly
instead of converting to a tibble, for bulk-analysis users who don't want
even the boundary conversion. Defaults to \code{FALSE}.}

\item{limit}{Maximum number of rows to return. \code{NULL} (default) returns all
matching rows.}

\item{checkpoint}{Optional path to a checkpoint file for resumable pulls (see
\code{\link[=estat_stats_data]{estat_stats_data()}}).}

\item{geometry}{If \code{TRUE}, join e-Stat boundary polygons onto the result by
\code{area_code} and return an \link[sf:sf]{sf} object for choropleth mapping (see
\code{\link[=estat_join_geometry]{estat_join_geometry()}}). Requires the \pkg{sf} package and
\code{decode_labels = TRUE}. Defaults to \code{FALSE}.}

\item{geometry_level, geometry_year, geometry_datum, geometry_designated_cities}{Passed to \code{\link[=estat_join_geometry]{estat_join_geometry()}} when \code{geometry = TRUE}. Match
\code{geometry_year} to the census year of your data.}

\item{lang}{Label language: \code{"E"} for English (the package default, settable
with \code{options(estatr.lang = )}) or \code{"J"} for Japanese. Tables that have no
English release fall back to Japanese automatically, with a warning.}

\item{key}{e-Stat appId. Defaults to the stored key.}
}
\value{
A tidy \link[tibble:tibble]{tibble} (or \code{data.table} if \code{as_data_table},
or an \link[sf:sf]{sf} object if \code{geometry = TRUE}), with a \code{notes}
attribute holding the table's annotation legend.
}
\description{
The main entry point. Given a \code{statsDataId}, fetches the data and its
classification metadata in one call, decodes every numeric code to a
human-readable label, and returns a tidy tibble: one row per observation with
paired label/code columns for each classification axis (e.g. \code{area} +
\code{area_code}, \code{time} + \code{time_code}), the \code{unit}, a numeric \code{value}, and an
\code{annotation} column preserving any non-numeric markers (suppressed cells,
footnote symbols) instead of silently coercing them to \code{NA}.
}
\examples{
\dontrun{
estat_api_key("your-app-id")
# Labour Force Survey, one category, decoded to labels
get_estat("0003217721", cdCat03 = "1", limit = 500)
}
}
