% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot.R
\name{pivot_estat_wide}
\alias{pivot_estat_wide}
\title{Pivot tidy e-Stat output to wide form}
\usage{
pivot_estat_wide(data, names_from, values_from = "value", id_cols = NULL)
}
\arguments{
\item{data}{A tibble or data.frame from \code{\link[=get_estat]{get_estat()}} (with \code{decode_labels}).}

\item{names_from}{Column whose values become the new column names (e.g.
\code{"cat01"}). Its paired \verb{_code} column is dropped from the id set.}

\item{values_from}{Column holding the cell values to spread. Defaults to
\code{"value"}.}

\item{id_cols}{Columns identifying each output row. Defaults to every column
except \code{names_from} (and its \verb{_code} partner), \code{values_from}, and
\code{annotation}.}
}
\value{
A wide \link[tibble:tibble]{tibble}, one row per unique combination of
\code{id_cols}.
}
\description{
Reshapes the long/tidy output of \code{\link[=get_estat]{get_estat()}} into wide form, spreading the
labels of one classification axis across columns. Mirrors tidycensus's
\code{output = "wide"}, but as an explicit opt-in step rather than the default.
}
\examples{
\dontrun{
d <- get_estat("0003217721", limit = 500)
pivot_estat_wide(d, names_from = "cat01")
}
}
