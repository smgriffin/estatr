% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_data.R
\name{estat_stats_data}
\alias{estat_stats_data}
\title{Retrieve statistical data values from e-Stat}
\usage{
estat_stats_data(
  statsDataId,
  ...,
  limit = NULL,
  start_position = 1L,
  checkpoint = NULL,
  lang = getOption("estatr.lang", "E"),
  key = get_estat_key()
)
}
\arguments{
\item{statsDataId}{The table id to retrieve (a ~10-digit string).}

\item{...}{Filter parameters passed to \code{getStatsData}, e.g. \code{cdCat01},
\code{cdArea}, \code{cdTime}, \code{cdTimeFrom}, \code{cdTimeTo}. Vectors are comma-joined into
e-Stat's expected code-list form.}

\item{limit}{Maximum number of rows to return. \code{NULL} (default) returns all
matching rows, paginating as needed.}

\item{start_position}{1-based absolute row offset to start from.}

\item{checkpoint}{Optional path to a checkpoint file for resumable pulls. When
set, each page's rows are persisted keyed by absolute offset, so an
interrupted large pull resumes by re-requesting only the missing pages.}

\item{lang}{Label language: \code{"E"} for English (the package default, settable
with \code{options(estatr.lang = )}) or \code{"J"} for Japanese. Tables that have no
English release fall back to Japanese automatically, with a warning.}

\item{key}{e-Stat appId. Defaults to the stored key.}
}
\value{
A \link[tibble:tibble]{tibble} of coded data values.
}
\description{
Low-level wrapper around the e-Stat \code{getStatsData} endpoint. Given a
\code{statsDataId} (from \code{\link[=estat_stats_list]{estat_stats_list()}}) and optional filter codes, returns a
tibble of the raw data values, one row per observation, with e-Stat's numeric
classification codes intact (\code{tab}, \code{cat01}, ..., \code{area}, \code{time}, \code{unit},
\code{value}).
}
\details{
Pagination is automatic: e-Stat caps each response at 100,000 records, and
this function fetches the remaining pages concurrently (bounded, throttled)
using the total record count reported on the first page. For decoded,
analysis-ready output with human-readable labels, use \code{\link[=get_estat]{get_estat()}}.
}
\examples{
\dontrun{
estat_api_key("your-app-id")
estat_stats_data("0003217721", cdCat03 = "1", limit = 100)
}
}
