% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_estat.R
\name{search_estat}
\alias{search_estat}
\title{Search the e-Stat catalog (interactive-friendly)}
\usage{
search_estat(
  keyword = NULL,
  gov_org = NULL,
  updated_from = NULL,
  updated_to = NULL,
  limit = 100L,
  ...,
  lang = getOption("estatr.lang", "E"),
  key = get_estat_key()
)
}
\arguments{
\item{keyword}{Keyword(s) to search for. Japanese is supported. Combine terms
with \code{AND}/\code{OR} per the e-Stat API.}

\item{gov_org}{Government organisation code to filter by (passed as
\code{statsCode}'s org prefix is not assumed; use \code{\link[=estat_stats_list]{estat_stats_list()}} for full
control).}

\item{updated_from, updated_to}{Optional update-date bounds (\code{yyyymmdd} or
\code{yyyymm}) to restrict to recently refreshed tables.}

\item{limit}{Maximum number of tables to return (default 100).}

\item{...}{Further parameters passed through to \code{\link[=estat_stats_list]{estat_stats_list()}}.}

\item{lang}{Label language: \code{"E"} for English (the package default, settable
with \code{options(estatr.lang = )}) or \code{"J"} for Japanese. Tables that have no
English release fall back to Japanese automatically, with a warning.}

\item{key}{e-Stat appId. Defaults to the stored key.}
}
\value{
A \link[tibble:tibble]{tibble} of matching tables, friendly columns first.
}
\description{
A friendlier wrapper over \code{\link[=estat_stats_list]{estat_stats_list()}} for finding tables
interactively. Returns a tibble with the most useful columns renamed to
stable snake_case (\code{id}, \code{stat_name}, \code{title}, \code{gov_org}, \code{survey_date}, ...)
and moved to the front, with the raw columns kept after them.
}
\examples{
\dontrun{
search_estat("労働力調査")
search_estat("国勢調査", updated_from = "2020")
}
}
