% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_catalog.R
\name{estat_data_catalog}
\alias{estat_data_catalog}
\title{Search the e-Stat data catalog for datasets and files}
\usage{
estat_data_catalog(searchWord = NULL, ..., key = get_estat_key())
}
\arguments{
\item{searchWord}{Keyword(s) to search. Japanese is supported.}

\item{...}{Further query parameters passed to \code{getDataCatalog} verbatim
(e.g. \code{statsCode}, \code{dataType}, \code{limit}, \code{startPosition}).}

\item{key}{e-Stat appId. Defaults to the stored key.}
}
\value{
A \link[tibble:tibble]{tibble} of catalog entries, one row per entry.
Returns a zero-row tibble when nothing matches.
}
\description{
Wraps the e-Stat \code{getDataCatalog} endpoint, which returns catalog entries —
datasets and their downloadable resources (Excel/CSV/PDF URLs) — rather than
machine-readable data values. Use \code{\link[=estat_stats_data]{estat_stats_data()}} / \code{\link[=get_estat]{get_estat()}} for the
actual numbers.
}
\examples{
\dontrun{
estat_data_catalog(searchWord = "国勢調査", limit = 10)
}
}
