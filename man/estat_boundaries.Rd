% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{estat_boundaries}
\alias{estat_boundaries}
\title{Download e-Stat administrative boundaries as an sf object}
\usage{
estat_boundaries(
  areas = NULL,
  level = c("municipality", "prefecture", "small_area"),
  year = 2020,
  datum = c("2000", "2011"),
  cache = TRUE,
  designated_cities = c("both", "ward", "city")
)
}
\arguments{
\item{areas}{Character vector of area codes selecting which prefectures to
download: 2-digit prefecture codes (e.g. \code{"31"}) or any codes whose first
two digits are a prefecture (e.g. a 5-digit \code{"31201"} or the \code{area_code}
column from \code{\link[=get_estat]{get_estat()}}). \code{NULL} downloads all 47 prefectures (large).}

\item{level}{Geographic level to return: \code{"municipality"} (default; 5-digit
\code{PREF+CITY}) or \code{"prefecture"} (dissolved whole prefectures, \code{PREF+"000"})
or \code{"small_area"} (raw 町丁・字, 9-digit \code{KEY_CODE}).}

\item{year}{Census year of the boundaries (e.g. \code{2020}, \code{2015}). Match this to
the census year of your data: municipality codes and boundaries change
between censuses (mergers), so a mismatched year can mis-join.}

\item{datum}{Geodetic datum: \code{"2000"} (JGD2000, EPSG:4612, default) or
\code{"2011"} (JGD2011, EPSG:6668).}

\item{cache}{If \code{TRUE} (default), cache downloaded boundary files under
\code{\link[=estat_cache_dir]{estat_cache_dir()}}.}

\item{designated_cities}{How to handle the 20 ordinance-designated cities and
Tokyo's special wards at \code{level = "municipality"}, since e-Stat's shapefiles
carry only ward codes: \code{"both"} (default) returns ward polygons \emph{and} a
unioned parent-city polygon (e.g. both \code{01101}… and \code{01100} 札幌市), so data
coded at either level joins; \code{"ward"} returns wards only; \code{"city"} returns
the parent city only. Ignored for other levels.}
}
\value{
An \link[sf:sf]{sf} object with \code{area_code}, name columns, and geometry.
}
\description{
Fetches official e-Stat census boundary polygons and returns them as an
\pkg{sf} object with an \code{area_code} column matching the codes used by
\code{\link[=get_estat]{get_estat()}}, so
the two join cleanly. Boundaries are derived from e-Stat's authoritative
small-area (町丁・字) shapefiles and dissolved to the requested \code{level}.
}
\details{
Requires the \pkg{sf} package. Downloaded files are cached (see
\code{\link[=estat_cache_dir]{estat_cache_dir()}}).
}
\examples{
\dontrun{
# Municipalities of Tottori (prefecture 31), 2020 census boundaries
bnd <- estat_boundaries("31", level = "municipality", year = 2020)
}
}
