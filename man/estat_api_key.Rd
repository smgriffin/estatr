% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{estat_api_key}
\alias{estat_api_key}
\title{Set your e-Stat API key}
\usage{
estat_api_key(key, install = FALSE, overwrite = FALSE)
}
\arguments{
\item{key}{Your e-Stat \code{appId}, a string.}

\item{install}{If \code{TRUE}, write the key to \code{.Renviron} so it persists across
sessions. Defaults to \code{FALSE} (session only).}

\item{overwrite}{If \code{TRUE}, replace an existing \code{ESTAT_API_KEY} entry in
\code{.Renviron}. Ignored when \code{install = FALSE}.}
}
\value{
Invisibly, the key (so it can be piped), though it is never printed.
}
\description{
Registers your e-Stat \code{appId} for the current session, and optionally writes
it to your \code{.Renviron} so it is available in future sessions. The e-Stat API
requires a free \code{appId}; obtain one at \url{https://www.e-stat.go.jp/api/}.
}
\details{
The key is a secret. This function never prints it, and you should never
commit it to version control or paste it into issues. If a key is ever
exposed, delete and reissue it on the e-Stat mypage (up to 3 are allowed per
account).
}
\examples{
\dontrun{
# Session only
estat_api_key("your-app-id")

# Persist for future sessions
estat_api_key("your-app-id", install = TRUE)
}
}
