% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{estat_join_geometry}
\alias{estat_join_geometry}
\title{Attach boundary geometry to e-Stat data}
\usage{
estat_join_geometry(
  data,
  level = c("auto", "municipality", "prefecture", "small_area"),
  year = 2020,
  datum = c("2000", "2011"),
  designated_cities = c("both", "ward", "city")
)
}
\arguments{
\item{data}{A tibble from \code{\link[=get_estat]{get_estat()}} (decoded, with an \code{area_code} column).}

\item{level}{Geographic level, or \code{"auto"} (default) to infer: \code{"prefecture"}
if every \code{area_code} ends in \code{"000"}, otherwise \code{"municipality"}.}

\item{year, datum, designated_cities}{Passed to \code{\link[=estat_boundaries]{estat_boundaries()}}. Match
\code{year} to your data's census year. \code{designated_cities} defaults to \code{"both"}
so data coded at either ward or parent-city level joins.}
}
\value{
An \link[sf:sf]{sf} object: the input columns plus a \code{geometry} column.
}
\description{
Joins e-Stat boundary polygons onto a tidy \code{\link[=get_estat]{get_estat()}} result by
\code{area_code}, returning an \pkg{sf} object ready for choropleth mapping. Each data
row receives its area's geometry (so a long time series repeats geometry per
period, matching the tidycensus long-plus-geometry convention).
}
\examples{
\dontrun{
d <- get_population_census(cdCat01 = "0")
sf_d <- estat_join_geometry(d, level = "prefecture", year = 2020)
}
}
