% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_list.R
\name{estat_stats_list}
\alias{estat_stats_list}
\title{Search the e-Stat catalog for statistical tables}
\usage{
estat_stats_list(
  searchWord = NULL,
  statsCode = NULL,
  surveyYears = NULL,
  statsField = NULL,
  searchKind = NULL,
  limit = NULL,
  startPosition = NULL,
  ...,
  key = get_estat_key()
)
}
\arguments{
\item{searchWord}{Keyword(s) to search. Japanese is supported and encoded as
UTF-8. Combine terms with \code{AND}/\code{OR} per the e-Stat API.}

\item{statsCode}{Government statistics code to filter by (5 or 8 digits).}

\item{surveyYears}{Survey period filter: \code{yyyy}, \code{yyyymm}, or
\code{yyyymm-yyyymm}.}

\item{statsField}{Statistics field code (2 or 4 digits).}

\item{searchKind}{Data kind: \code{1} (statistics, default) or \code{2} (regional
statistics / sub-datasets).}

\item{limit}{Maximum number of tables to return.}

\item{startPosition}{1-based row offset to start from.}

\item{...}{Further query parameters passed through to \code{getStatsList}
verbatim (e.g. \code{updatedDate}, \code{openYears}).}

\item{key}{e-Stat appId. Defaults to the stored key.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per matching table. Returns a
zero-row tibble when the search matches nothing.
}
\description{
Low-level wrapper around the e-Stat \code{getStatsList} endpoint. Searches the
government-wide statistics catalog and returns a tibble of matching tables,
one row per table, including the \code{id} (the \code{statsDataId} you will pass to
\code{estat_stats_data()} once that wrapper is implemented).
}
\details{
This is a power-user function that mirrors the API closely. Higher-level,
friendlier search (\code{search_estat()}) is planned for a later milestone.
}
\examples{
\dontrun{
estat_api_key("your-app-id")
# Tables mentioning the Labour Force Survey
estat_stats_list(searchWord = "労働力調査")
}
}
